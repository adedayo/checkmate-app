import { Environment } from './ienvironment';

export const environment: Environment = {
  production: false,
  apiHost: 'localhost',
  apiPath: 'api',
  apiPort: 17283,
  wsProtocol: 'ws',
  httpProtocol: 'http'
};
